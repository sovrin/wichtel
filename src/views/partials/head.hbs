<meta charset="UTF-8">

<link rel="stylesheet" href="//unpkg.com/tacit-css"/>
<link rel="stylesheet" href="//unpkg.com/tachyons"/>
<link rel="icon" type="image/x-icon" href="/images/favicon.ico">

<style>
    html {
        --patternColor: #F2F2F2;
        --backgroundColor: #FAFAFA;
        background: var(--backgroundColor);
    }

    body {
        min-height: 100vh;

        background: radial-gradient(circle at top left, transparent 9%, var(--patternColor) 10%, var(--patternColor) 15%, transparent 16%),
        radial-gradient(circle at bottom left, transparent 9%, var(--patternColor) 10%, var(--patternColor) 15%, transparent 16%),
        radial-gradient(circle at top right, transparent 9%, var(--patternColor) 10%, var(--patternColor) 15%, transparent 16%),
        radial-gradient(circle at bottom right, transparent 9%, var(--patternColor) 10%, var(--patternColor) 15%, transparent 16%),
        radial-gradient(circle, transparent 25%, var(--backgroundColor) 26%),
        linear-gradient(45deg, transparent 46%, var(--patternColor) 47%, var(--patternColor) 52%, transparent 53%),
        linear-gradient(135deg, transparent 46%, var(--patternColor) 47%, var(--patternColor) 52%, transparent 53%);
        background-size: 2em 2em;
    }

    footer {
        background: rgba(var(--backgroundColor), 0.8);
        backdrop-filter: blur(3px);

    }
</style>
